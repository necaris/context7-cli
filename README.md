# Context7 Documentation: CLI

Use the API reference to implement a minimal, fast CLI client for Context7. Should be less context-polluting than the MCP, although the MCP is pretty lightweight.

Caching, etc etc are nice-to-haves too

1. Nim?
2. Hare?
3. Go / Rust
